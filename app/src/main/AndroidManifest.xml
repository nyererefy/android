<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.konektedi.vs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Elections.ElectionView"
            android:label="@string/title_activity_election_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Elections.Results.ResultsView"
            android:label="@string/title_activity_results_view"
            android:parentActivityName=".Elections.ElectionView"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.konektedi.vs.Elections.ElectionView" />
        </activity>
    </application>

</manifest>