subscription countsSubscription($subcategoryId: Int!) {
    candidateAndVotesCount(subcategoryId: $subcategoryId) {
        id
        votesCount
        avatar
        user {
            name
        }
    }
}