subscription countsSubscription($subcategoryId: Int!) {
    candidatesAndVotesCount(subcategoryId: $subcategoryId) {
        id
        votesCount
        avatar
        user {
            name
        }
    }
}